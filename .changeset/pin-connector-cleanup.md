---
"catalog": patch
---

Reclassify 70 mislabeled `connection: pin` ports to their real connectors (binding-post, db25, hdmi, pin-header, 1/8-inch, xlr) and add a `suspect-pin` dataset-audit check that flags the remaining pin ports for review (AUREO-1043).
