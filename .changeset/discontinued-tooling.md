---
"catalog": minor
---

Add `discontinued:report` and `discontinued:apply` scripts plus a
monthly CI check that surfaces and auto-tags catalog entries on the
receiving end of a `supersedes` link but missing the `discontinued`
category.
